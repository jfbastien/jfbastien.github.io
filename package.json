{
  "private": true,
  "packageManager": "bun@1.3.10",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build:site": "bun run site/build.ts",
    "build:og": "bun run social/generate.ts",
    "build:assemble": "bun run site/assemble.ts",
    "build:screenshot": "bun run site/screenshot.ts",
    "build": "bun run build:site && bun run build:og && bun run build:assemble && bun run build:screenshot"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "bun-types": "1.3.10",
    "puppeteer-core": "^24.37.5",
    "satori": "^0.12.0",
    "subset-font": "^2.4.0",
    "typescript": "^5.7.0",
    "ultrahtml": "^1.6.0"
  }
}
